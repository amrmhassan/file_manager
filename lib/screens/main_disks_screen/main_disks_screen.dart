import 'package:flutter/material.dart';

class MainDisksScreen extends StatelessWidget {
  const MainDisksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
